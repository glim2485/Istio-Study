apiVersion: v1
kind: Service
metadata:
  name: servicea
  namespace: servicea
  labels:
    app: servicea
spec:
  type: ClusterIP
  selector:
    app: servicea
  ports:
    - protocol: TCP
      port: 6700
      targetPort: 6700

---

apiVersion: v1
kind: Service
metadata:
  name: servicesuba
  namespace: servicea
  labels:
    app: servicesuba
spec:
  type: ClusterIP
  selector:
    app: servicesuba
  ports:
    - protocol: TCP
      port: 6789
      targetPort: 6789

---

apiVersion: v1
kind: Service
metadata:
  name: serviceb
  namespace: serviceb
  labels:
    app: serviceb
spec:
  type: ClusterIP
  selector:
    app: serviceb
  ports:
    - protocol: TCP
      port: 6701
      targetPort: 6701

---

apiVersion: v1
kind: Service
metadata:
  name: servicec
  namespace: servicec
  labels:
    app: servicec
spec:
  type: ClusterIP
  selector:
    app: servicec
  ports:
    - protocol: TCP
      port: 6702
      targetPort: 6702